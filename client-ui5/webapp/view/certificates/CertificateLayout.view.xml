<mvc:View
    controllerName="invoice.app.controller.certificates.CertificateLayout"
    xmlns="sap.m"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns:tnt="sap.tnt"
    xmlns:core="sap.ui.core"
    displayBlock="true"
    height="100%">
    
    <tnt:ToolPage id="certificateToolPage">
        <tnt:header>
            <tnt:ToolHeader class="bannerHeader">
                <Button icon="sap-icon://menu2" type="Transparent" press="onMenuPress" tooltip="Menu" class="sapUiSmallMarginEnd" visible="{ui>/certificateMenuVisible}"/>
                <Title text="Gestor de Certificados" level="H2" class="sapUiSmallMarginBegin"/>
                <ToolbarSpacer/>
                <Button icon="sap-icon://search" type="Transparent" tooltip="{i18n>search}"/>
                <Button icon="sap-icon://bell" type="Transparent" tooltip="Notifications"/>
                <Avatar src="img/profile_pic.png" displaySize="XS" press="onProfilePress" class="sapUiSmallMarginBegin" tooltip="Profile"/>
            </tnt:ToolHeader>
        </tnt:header>

        <!-- Side Navigation -->
        <tnt:sideContent>
            <tnt:SideNavigation
                expanded="true"
                itemSelect="onItemSelect"
                visible="{ui>/certificateSidebarVisible}">
                <tnt:NavigationList>
                    <tnt:NavigationListItem
                        text="Listado de Certificados"
                        icon="sap-icon://certificate"
                        key="certificateList"/>
                </tnt:NavigationList>
            </tnt:SideNavigation>
        </tnt:sideContent>
        
        <!-- Main Content -->
        <tnt:mainContents>
            <NavContainer id="certificateNavContainer" height="100%">
                <pages>
                    <!-- Default page will be loaded via manifest -->
                </pages>
            </NavContainer>
        </tnt:mainContents>
    </tnt:ToolPage>
</mvc:View>
