specVersion: "3.0"
metadata:
  name: invoice.app
type: application
framework:
  name: OpenUI5
  version: "1.120.0"
  libraries:
    - name: sap.m
    - name: sap.ui.core
    - name: sap.ui.layout
    - name: themelib_sap_horizon
server:
  customMiddleware:
    - name: ui5-middleware-simpleproxy
      afterMiddleware: compression
      mountPath: /api
      configuration:
        baseUri: "http://localhost:3000/api"
    - name: ui5-middleware-simpleproxy
      afterMiddleware: compression
      mountPath: /api-docs
      configuration:
        baseUri: "http://localhost:3000/api-docs"
